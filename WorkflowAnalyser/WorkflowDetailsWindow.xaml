<Window x:Class="WorkflowAnalyser.WorkflowDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WorkflowAnalyser"
        mc:Ignorable="d"
        Title="WorkflowDetailsWindow" Height="650" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Border Grid.Row="2" Padding="12" Background="#11000000">
            <DockPanel>
                <StackPanel DockPanel.Dock="Left" Margin="0,0,12,0">
                    <TextBlock FontSize="16" FontWeight="SemiBold" Text="{Binding Index, StringFormat=Workflow: {0}}" />
                    <TextBlock FontSize="16" Text="{Binding FunctionDocHeader}" TextWrapping="Wrap" />
                    <Button Padding="10,6" Content="Show Descripotion of Case/FunctionIndex" IsEnabled="{Binding HasFunctionDoc}" Click="OpenFunctionDoc_Click" HorizontalAlignment="Left" Margin="0,5,0,0"/>
                </StackPanel>
            </DockPanel>
        </Border>
        
        <ScrollViewer Grid.Row="4" Margin="12">
            <ItemsControl ItemsSource="{Binding Rows}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderThickness="0,0,0,1" BorderBrush="#22000000" Padding="0,6">
                            <StackPanel>
                                <TextBlock FontWeight="SemiBold" Text="{Binding Key}" />
                                <TextBlock Text="{Binding Value}" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>
