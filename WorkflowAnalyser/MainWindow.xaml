<Window x:Class="WorkflowAnalyser.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Workflow Viewer" Width="600" Height="210">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock FontSize="16" FontWeight="SemiBold" Text="Workflow Analyser" />

        <StackPanel Grid.Row="4" Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock VerticalAlignment="Center" Text="Depth-Incoming: "/>
            <TextBox x:Name="DepthBoxIncoming" Width="50" Text="10" VerticalAlignment="Center"/>

            <TextBlock VerticalAlignment="Center" Text="Index: " Margin="14,0,0,0"/>
            <TextBox x:Name="StartIndexBox" Width="50" Text="555" VerticalAlignment="Center"/>

            <TextBlock VerticalAlignment="Center" Text="Depth-Outgoing: " Margin="14,0,0,0"/>
            <TextBox x:Name="DepthBoxOutgoing" Width="50" Text="10" VerticalAlignment="Center"/>

            <Button Content="Graph öffnen" Margin="14,0,0,0" Padding="14,6" Click="OpenGraph_Click"/>
        </StackPanel>
    </Grid>
</Window>
